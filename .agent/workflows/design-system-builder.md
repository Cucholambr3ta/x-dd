---
description: Workflow X-DD - design-system-builder
---

# /design-system-builder

> **Estandar de documentacion:** Todo artefacto que produzca este workflow cumple
> [`docs/DOC_STANDARD.md`](../../docs/DOC_STANDARD.md): sin emojis, diagramas Mermaid
> obligatorios, tablas para datos estructurados, Gherkin donde aplique, secciones
> minimas y trazabilidad bidireccional.

**ID:** FLUJO-084 | **Versión:** 2.7.2 | **Nivel:** Operativo
**Módulo Core:** `skill-design-system`, `skill-ui-styling`
**Orquestador:** Orchestrator (ID: 00)
**Misión:** Extraer, abstraer y consolidar los componentes aprobados en un repositorio unificado y reutilizable.

## 0. PRE-FLIGHT: MEMORY SEAL (START)
- Registro obligatorio en `memoria.md`.

## 1. MISIÓN DEL FLUJO
Constituir la librería oficial de tokens y componentes (`design/design_system.md` y `styles/globals.css`). Transforma el arte élite en un framework repetible de ingeniería UI, escalable para React o equivalentes.

## 2. DIRECTRICES INQUEBRANTABLES
- **Aislamiento DRY**: Todo token de diseño debe ir centralizado. Nada de estilos en línea o divergentes.
- **Consistencia Visual**: Se debe testear que los tokens repliquen las propuestas aprobadas en `/elite-designer-generator`.

## 3. DOMINIOS DE CONTROL X-DD
Auditoría y formalización matemática del diseño (escalas tipográficas, espaciados, colores semánticos, breakpoints).

## 4. CONEXIONES DE INTEROPERABILIDAD (ART. 6)
- **Predecesores:** Aprobación Humana de `/elite-designer-generator`.
- **Sucesores:** `/pencil-mockup-architect`, `/plan-fases` (para desarrollo iterativo).
- **Skills Vinculadas:** `skill-design-system`, `skill-ui-styling`.

---
**Versión:** 2.7.2
X-DD System

## POST-FLIGHT: MEMORY SEAL (END)
- Cierre de sesión y persistencia final en `memoria.md`.
