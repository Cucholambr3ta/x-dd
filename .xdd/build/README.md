# .xdd/build — Fase Build

Artefactos de la fase Build del pipeline X-DD para este proyecto.

> Los reportes históricos de sprints 3–13 (cuando X-DD se autoconstruyó) se archivaron
> en [docs/archive/sprints/](../../docs/archive/sprints/) por trazabilidad. No son
> artefactos vivos de la fase.

La evidencia de build vigente (run-evidence, flow-trace si aplica) se genera vía
`/cierre-fase` o `xdd-flow.py` al cerrar la fase.
